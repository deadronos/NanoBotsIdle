# PowerShell (summary)

From `.github/instructions/powershell.instructions.md`:

- Use full cmdlet names (avoid aliases) in scripts.
- Document parameters and pipeline inputs; avoid interactive prompts in automation.
- Avoid `Write-Host` for data output; prefer writing structured output that can be parsed.
- Be explicit about parameter types and validate inputs early.
