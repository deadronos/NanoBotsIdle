import { describe } from "vitest";

describe.skip("DynamicResScaler integration (disabled)", () => {
  // disabled — see tests/dynamic-res-scaler.integration.test.tsx for a more robust, skipped integration test
});

