// Shared runtime constants used across rendering and simulation
// Keep values aligned with current behavior in the codebase
export const BASE_SEED = 123;
export const PRESTIGE_SEED_DELTA = 99;
export const WORLD_RADIUS = 30;
export const WATER_LEVEL = 0.2;

export const PLAYER_HEIGHT = 1.8;

// Player movement/physics
export const WALKING_SPEED = 5.0;
export const RUNNING_SPEED = 8.0;
export const JUMP_FORCE = 8.0;
export const GRAVITY = 20.0;
